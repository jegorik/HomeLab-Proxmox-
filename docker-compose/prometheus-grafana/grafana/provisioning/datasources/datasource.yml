# Grafana Datasource Provisioning
# This file auto-configures Prometheus as a data source on Grafana startup.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

