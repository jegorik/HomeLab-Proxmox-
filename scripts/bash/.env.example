# =============================================================================
# Ansible User Setup - Environment Configuration Example
# =============================================================================
#
# Copy this file to .env and customize the values for your environment.
#
# Usage:
#   cp .env.example .env
#   nano .env  # Edit with your SSH key
#   chmod 600 .env
#
# SECURITY WARNING:
#   - Never commit .env with real SSH keys to version control
#   - Ensure .env has restricted permissions (chmod 600)
#
# =============================================================================

# -----------------------------------------------------------------------------
# User Configuration
# -----------------------------------------------------------------------------

# Username for the Ansible automation account
ANSIBLE_USER="ansible"

# User shell
ANSIBLE_SHELL="/bin/bash"

# Enable passwordless sudo for automation (true/false)
ANSIBLE_SUDO="true"

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------

# SSH public key for authentication (REQUIRED)
# Generate a new key pair:
#   ssh-keygen -t ed25519 -C "ansible@semaphore" -f ~/.ssh/ansible_key
#
# Then paste the PUBLIC key content here:
ANSIBLE_SSH_KEY="ssh-ed25519 AAAA... your-comment-here"
